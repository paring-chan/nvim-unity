# Neovim Unity

Use Neovim as Unity's external code editor!

Currently supports only MacOS(and maybe linux, neovide only) in default configuration because I don't have any other systems than my Macbook Pro for now.

## Installation

Add this to unity package manager:

```
git+https://github.com/paring-chan/nvim-unity.git
```

